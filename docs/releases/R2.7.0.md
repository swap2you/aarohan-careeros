# R2.7.0 — Gmail Lifecycle

**Tag:** `r2.7.0`  
**Depends on:** r2.6.1

## Delivered

- Extended Gmail labels (LinkedIn, Indeed, Dice, Glassdoor, USAJOBS, lifecycle folders)
- Per-source job-alert parsers with fixture corpus (`apps/api/fixtures/gmail/messages.json`)
- Lifecycle classifier (job alert, recruiter, confirmation, assessment, interview, rejection, offer, follow-up)
- Alert → `ingest_job()` pipeline with API/aggregator deduplication
- `RecruiterSignal` metadata: message/thread IDs, snippet (no raw body in API), company/application links
- User classification correction (`PATCH /api/recruiter-signals/{id}/classification`)
- Fixture + live sync idempotency via `processed_gmail_messages`
- Gmail Lifecycle UI on `/recruiter-signals`

## Migration

- `0008_r27_gmail_lifecycle` — extends `recruiter_signals`

## Tests

- 116 API tests (container)
- 17 Playwright tests (including R2.7 fixture sync)

## Next

R2.8 Interview and Recruiter Intelligence
