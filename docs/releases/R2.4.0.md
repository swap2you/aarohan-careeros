# R2.4.0 — Career Evidence Vault and Document Quality

Date: 2026-06-28  
Tag: `r2.4.0`  
Prior tag: `r2.3.0` (`dfcc391a6520341233b3f8ceeccefafda98e6e20`)

## Objective

Evidence-grounded document generation with ATS diagnostics, claim validation, answer sheets, and deterministic fallback.

## Delivered

- `config/document-templates.yml` — versioned template/prompt/model metadata
- `document_quality.py` — ATS diagnostics, DOCX/PDF comparison, claim validation, answer sheet
- `/api/documents` — templates, baseline resume hash, quality report, validate
- Packet generation stores `document_quality` and `answer_sheet` in metadata
- Applications UI — quality PASS/FAIL and report viewer
- Tests: `tests/test_document_quality.py` (8 scenarios)

## Tests / gate results

| Check | Result |
|-------|--------|
| secret_scan | PASS |
| prohibited_source_scan | PASS |
| pytest | 58 passed, 1 skipped |
| web build | PASS |

## Known limitations

- PDF comparison skipped when WeasyPrint unavailable (text fallback artifact)
- Full DOCX contradiction parsing deferred; claims validated against evidence registry
- AI generation remains budget-gated; default mode is deterministic template fill

## Rollback

```powershell
git checkout r2.3.0
# No new migrations in this release
```
