# R2.3.0 — Trust, Matching, and Explainability

Date: 2026-06-28  
Tag: `r2.3.0`  
Commit: `dfcc391a6520341233b3f8ceeccefafda98e6e20`  
Prior tag: `r2.2.0` (`81aef74c73b783dd34117674ff0e0aca15940bf1`)

## Objective

Add profile-driven hard filters, trust/fit explainability, role-family classification, job aging, and human-readable job cards.

## Delivered

- Migration `0005_trust_matching`: role_family, expiration, trust/fit reasons, match_card JSON
- `trust_matching.py` — hard filters, trust score, fit reasons, duplicate-source notes, match cards
- Integrated into `score_job()` on every ingest/rescore
- `/api/matching` — preferences, match card, rescore
- Jobs UI — fit/trust/filter columns and match summaries
- Tests: `tests/test_trust_matching.py` (6 scenarios)

## Tests / gate results

| Check | Result |
|-------|--------|
| secret_scan | PASS |
| prohibited_source_scan | PASS |
| pytest | 50 passed, 1 skipped |
| web build | PASS |

## Known limitations

- Work authorization hard filter uses `TODO_VERIFY` placeholder until career vault confirms statement
- Trust scoring is rule-based; no ML similarity yet
- Job expiration uses fixed 60/90-day defaults (not yet configurable in Settings UI)

## Rollback

```powershell
git checkout r2.2.0
cd apps/api
alembic downgrade 0004_duplicate_protection
```
