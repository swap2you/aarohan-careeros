# R2.10 — Modern UI

**Tag:** `r2.10.0`

## Summary

Design-system pass across navigation, forms, cards, focus states, loading skeletons, and empty states. Reports dashboard uses human-readable metrics instead of raw JSON.

## Changes

- CSS design tokens (`globals.css`) — typography, spacing, focus rings, skeletons
- Reports page metric cards
- Consistent labels and action rows on Ask, Interviews, Gmail Lifecycle
- Protected-route auth splash preserved (no flash)

## Tests

- Expanded Playwright coverage on auth, Gmail, interviews, Ask flows
- `npm run build` gate
