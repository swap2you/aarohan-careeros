Continue the current Aarohan CareerOS gate until all acceptance criteria are met.

Do not stop after generating code.
Run formatting, linting, unit tests, integration tests, and relevant E2E tests.
Fix failures.
Update documentation.
Return actual evidence.

Stop only for:
- missing credential requiring user action;
- external account approval;
- destructive action;
- legal or billing decision;
- architecture conflict that cannot be resolved from repository policy.
