# Observability (R2.11)

## Local

- `GET /health` — API liveness
- Audit log table for sensitive operations
- AI budget tracking endpoints

## Cloud target

- Structured JSON logs
- Request correlation IDs
- Metrics: connector latency, Gmail sync duration, Ask/TTS cost
- Alerts on auth failure spikes and budget threshold
