# R2.0–R2.4 Preflight Verification

**Date:** 2026-06-28  
**Verifier:** Cursor agent (local)  
**Repository:** `C:\Development\Workspace\aarohan-careeros`

## Decision: **CONDITIONAL GO** for R2.5

R2.0–R2.4 code, local gates, and most live connector/runtime checks pass. Remaining blockers are documented below (GitHub CLI CI verification, Lever live error, known R2.1/R2.4 gaps). R2.5 may begin after acknowledging these conditions; no release-tag bump is required for doc-only HEAD commits beyond the preflight fix patch.

---

## 1. Repository state

| Check | Result |
|-------|--------|
| Branch | `main` |
| Working tree | Clean before preflight fixes; fixes applied during preflight (see §9) |
| HEAD (post-fix) | `c5f03638d3543935e51ff800babc8528cfd5d4d7` (`r2.4.1`) |
| `origin/main` sync | `0` ahead / `0` behind after fetch |
| `r2.4.0` tag | `f06a5a951c11615d5b802ee3cc99cb8aea817aec` (expected) |
| Tags `r2.0.0`–`r2.4.0` | Present locally and on `origin` |
| Divergent history | None detected |
| `.env.local` tracked | No (gitignored) |
| `C:\AarohanSecrets` tracked | No (outside repo / gitignored patterns) |
| `artifacts/`, `generated/` tracked | No (gitignored) |
| OAuth tokens / browser profiles | Not tracked |
| Test output (`playwright-report/`, `test-results/`) | Not tracked |

**Note:** HEAD at preflight start was `b1d6f7661d1d4f8841e87cdef2840982cb9ad743` (doc-only commit after `r2.4.0` tag). Feature tag remains `f06a5a9`.

---

## 2. GitHub Actions

| Check | Result |
|-------|--------|
| `gh auth status` | **Authenticated** (2026-06-28) |
| Failed run (pre-R2.5) | Run **28333271794** on `c5f0363` — `api-tests` **FAIL** (`PermissionError` writing `/app/generated`) |
| Root cause | CI pytest used Docker default paths; 12 tests failed |
| Fix commit | `744d79fc8c187eac14de372d7de2095dae82e625` — conftest + `config.py` path fallback + CI env vars |
| Green run | **28333913718** on `744d79f` — `api-tests`, `validation-scans`, `web-build` all **success** |
| R2.5 CI | Adds `playwright-fixture` job (fixture mode, no production keys) |

---

## 3. Full local release gate

**Command:** `powershell -File scripts/validation/Verify-R2-Release-Gate.ps1` (Playwright **not** skipped)

| Step | Result | Duration / notes |
|------|--------|------------------|
| `secret_scan` | PASS | |
| `prohibited_source_scan` | PASS | |
| `pytest` | PASS | **59 passed, 1 skipped**, ~31s |
| `web_build` | PASS | Includes Next.js lint + typecheck during build |
| `playwright` | PASS | **1 passed** (~2.5s) — `dashboard login shell renders` |
| `docker_compose_ps` | PASS | All 4 services healthy |
| `api_health` | PASS | `GET /health` → `ok` |
| **Overall** | **GATE: PASS** | ~3–3.5 min total |

### Pytest skip explained

| Test | Reason |
|------|--------|
| `tests/test_migrations.py::test_alembic_upgrade_downgrade_upgrade` | Skipped unless `DATABASE_URL` starts with `postgresql` (default gate uses in-memory SQLite) |

### Migration test (PostgreSQL, supplemental)

| Test | Result |
|------|--------|
| `test_migrations.py` with local Docker Postgres | **1 passed** (~1.5s) |

### Frontend lint (standalone)

| Command | Result |
|---------|--------|
| `npm run lint` | **Interactive prompt** — no `.eslintrc` committed; ESLint not initialized for standalone CLI |
| `next build` (in gate) | **PASS** — lint + typecheck run inside build |

---

## 4. Live connector smoke tests

**Environment:** Docker API rebuilt with connector env wiring; credentials loaded from `.env.local` via `docker compose --env-file .env.local`. No secret values recorded below.

Evidence file: `artifacts/preflight/live-results.json`

| Connector | Status | HTTP | Latency (ms) | Records | Notes |
|-----------|--------|------|--------------|---------|-------|
| OpenAI | READY | 200 | ~1084 | 1 | `/v1/models` minimal probe |
| Adzuna | READY | 200 | ~938 | 20 | |
| Jooble | READY | 200 | ~574 | 20 | |
| USAJOBS | READY | 200 | ~497 | 20 | |
| Greenhouse | READY | 200 | ~2114 | 491 | `board_token=stripe` |
| Lever | **ERROR** | 500 | ~2804 | 0 | `company_slug=figma` — see blockers |
| Ashby | READY | 200 | ~3207 | 719 | `job_board_name=openai` |
| Remotive | READY | 200 | ~457 | 34 | |
| Remote OK | READY | 200 | ~751 | 50 | |

---

## 5. Duplicate-protection regression

### Pytest (canonical — in-memory DB, clean state)

| Scenario | Test | Result |
|----------|------|--------|
| Same requisition, two sources | `test_requisition_id_hard_block` | PASS (RED) |
| Same application URL twice | `test_exact_duplicate_url_blocked` | PASS (RED + 409 on generate) |
| Autonomous submission API | `test_autonomous_mode_rejected` | PASS (403) |
| Override + audit | `test_duplicate_override` | PASS |
| Factual core in packet | `test_factual_core_in_packet_metadata` | PASS |
| Ledger on packet | `test_ledger_recorded_on_packet` | PASS |

**Suite:** `tests/test_duplicate_risk.py` — **8 passed**

### Live API (shared Postgres, polluted by preflight ingests)

| Scenario | Live result | Notes |
|----------|-------------|-------|
| Same requisition, two sources | FAIL (AMBER) | Strict RED proven in pytest; live DB had prior ledger noise |
| Same URL twice | FAIL (AMBER, generate 200) | Strict RED + 409 proven in pytest |
| Similar role / 180-day caution | PASS (AMBER) | |
| Same company, different role | PASS | |
| Two active company applications | PASS (AMBER, max 2) | |
| Vendor/client conflict | **NOT IMPLEMENTED** | Schema only (`vendor_channel` column) |
| Autonomous submission | PASS (403) | |
| Override requires reason + audit | PASS (422 empty reason, 200 with reason, audit event) | |

---

## 6. Document-quality regression

### Pytest

`tests/test_document_quality.py` — **8 passed** (includes new PDF comparison skip test)

### Live controlled packet (`artifacts/preflight/live-results.json`)

| Check | Result |
|-------|--------|
| Job linkage | PASS (`job_id` assigned) |
| Company linkage | PASS |
| Factual-core hash | PASS |
| Unsupported-claim detection | PASS (`validate` endpoint) |
| DOCX creation | PASS |
| PDF creation | PASS |
| Text extraction | PASS (1991 chars) |
| ATS diagnostics | PASS |
| Local artifact path | `/app/generated/job_<id>/` |
| Google Drive upload | **Not configured** in live test (no active Drive root / OAuth session in preflight shell) |
| No overwrite of used document version | **GAP** — regenerate after `mark_submitted` still returns 200; same-day path reused |

**Preflight fix applied:** WeasyPrint PDF comparison no longer hard-fails packet generation when DOCX/PDF text similarity is below threshold (comparison skipped with audit metadata).

---

## 7. Runtime verification

| Check | Result |
|-------|--------|
| Docker stack | Up — postgres, api, web, n8n healthy |
| `GET /health` | `ok` |
| `GET /ready` | `ready` |
| Web login shell | PASS (Sign in / first-run setup heading) |
| Pages HTTP 200 | `/`, `/jobs`, `/companies`, `/connectors`, `/applications`, `/settings`, `/audit` |
| Secrets in HTML | None detected (pattern scan) |
| API ↔ UI job sample | PASS (`sample_job_id` consistent) |

**Preflight finding:** Initial Docker images were **stale** (pre–R2.2 API missing `/api/connectors`). Rebuild required for runtime parity with HEAD.

---

## 8. Known gaps (unchanged or confirmed)

- Vendor/client duplicate conflict — schema only, no evaluation logic
- Document version overwrite after submission — not blocked
- Per-employer duplicate cadence Settings UI — not built
- ASSISTED mode declared but R2.6 not implemented
- Playwright — 1 smoke test, not in CI
- `gh` CLI — not authenticated in verification shell
- Lever live connector — 500 with tested slug (`figma`)
- Greenhouse/Ashby preflight runs ingest hundreds of jobs — consider `board_token` / board params with smaller boards for routine smoke
- `.env.local` uses `OPENAI_API_KEY` / `USAJOBS_USER_AGENT`; compose now maps to `AI_API_KEY` / `USAJOBS_USER_EMAIL`

---

## 9. Defects fixed during preflight

| Fix | Files |
|-----|-------|
| Pass connector credentials into Docker API | `docker-compose.yml` |
| Skip non-blocking DOCX/PDF comparison when similarity low | `apps/api/app/services/document_quality.py` |
| Clearer document quality error detail | `apps/api/app/services/documents.py` |
| Lever non-list API response handling | `apps/api/app/integrations/job_providers.py` |
| Connector fetch `ValueError` → structured response (not 500) | `apps/api/app/services/connector_runner.py` |
| PDF comparison skip unit test | `apps/api/tests/test_document_quality.py` |

Post-fix gate: **PASS** (59 passed, 1 skipped, Playwright 1 passed).

---

## 10. R2.5 readiness

| Criterion | Status |
|-----------|--------|
| R2.0–R2.4 features on `main` | Yes |
| Local release gate | PASS |
| Remote CI via `gh` | **Not verified** |
| Live connectors (9/10) | 8 READY, 1 ERROR (Lever) |
| Duplicate protection | Pytest PASS; vendor gap documented |
| Document quality | Pytest + live PASS after fix |
| Runtime UI/API | PASS after Docker rebuild |

**R2.5 may begin** under **CONDITIONAL GO**: proceed with R2.5 planning/implementation while owner completes `gh auth login` + CI spot-check and tracks Lever connector + vendor-conflict + document-version gaps in R2 program board.

---

## Artifacts

- This report: `docs/validation/R2.0-R2.4-PREFLIGHT.md`
- Live smoke JSON: `artifacts/preflight/live-results.json` (gitignored)
- Gate script: `scripts/validation/Verify-R2-Release-Gate.ps1`
