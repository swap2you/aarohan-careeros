# DEPRECATED — use docker-compose.test.yml via Start-Aarohan-E2E.ps1 / Run-Aarohan-Tests.ps1.
#
# This overlay shared owner postgres, credentials, network, and volume with career_os.
# It is retained only as a migration reference and must not be used for new test runs.
#
# Replacement:
#   pwsh scripts/local/Start-Aarohan-E2E.ps1
#   pwsh scripts/local/Run-Aarohan-Tests.ps1

services: {}
